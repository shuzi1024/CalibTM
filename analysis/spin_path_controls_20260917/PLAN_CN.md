# 单路径对照：7 小时执行计划

北京时间 2026-09-17 02:43:16 开始，09:43:16 截止；最多四张现有 H20。完成必要工作可提前结束。

## 固定问题与范围

检验显式本流真实观测读取与浅层 SPIN 上下文的组合，是否比任一单路径更值得使用。先 GEANT，三模型、种子 41001/41002。Full 保留旧 120 轮结果，不新增融合变体。

- SPIN+Direct：旧 spin_direct_no_memory 原模型，完整第 120 轮状态接续，不从最佳检查点重启。
- Context-only：保留 SPIN 上下文、归一化 Q 和原解码器；Direct/Sync 删除，解码器输入 [0,0,Q(H)]。
- Direct-only：保留本流最近真实观测双向读出、位置编码与解码器；查询取 Q(P)，删除 SPIN 聚合及观测值上下文编码。

这些是信息路径与使用价值对照，不是等参数控制，Context-only 不能代替官方 SPIN 基线。共享同名参数与旧初始化相同。

## 训练与评价

两新增对照独立从头训练，共四条；原组合两条接续。AdamW 学习率前 120 轮 0.001、后 40 轮 0.0001，其他原设置不变。保留原 120 轮截面与所有历史，不早停，不搜索新日程。

沿用原 512 拟合窗口、完整拟合段统计/相关图、20% 观测、三条件、数据顺序与掩码种子、原始缺失项 L1 损失。主指标：三个条件全局缺失 NMAE 等权均值；选全 160 轮开发分数严格改善的最早最佳检查点。NRMSE 为辅助指标。结果全部 development_only。160 轮不等于充分收敛。

保存逐种子、逐条件结果及逐轮轨迹。末 20 轮中位数、范围、相同轮配对胜负以及组合末 20 轮优于单路径全程最佳的轮数，只作解释，不替换预定主指标。不把相关缺失元素当独立重复。

## 顺序决策

先完成 GEANT 所有六条轨迹及匹配成本测量。组合在两种子上对两个单路径均有主指标增量，并得到末 20 轮中位数方向支持，才考虑用余下授权预算原样扩至 Abilene；此为研究路线筛选，不声称统计显著。不增设事后百分比门槛。若任一更简单路径达到组合水平，或证据仅依赖孤立最好点，则暂停组合路线，整理结果结束。各条件可异向，不要求所有条件同时获胜。

不自动展开完整基线重训、外部新模型、未核实新数据或再次改变融合。旧 Full 与旧 SPIN 只按其原协议参考列示。新独立评价来源与公开训练入口是未来投稿工作，不在本轮止损前扩张。

## 执行约束

原 source/output/checkpoint 只读，新代码与结果独立目录；续训恢复模型、AdamW 与 RNG。正式训练前检查隐藏真值不变性、单路径隔离、原组合输出/梯度兼容、GPU 两步训练与恢复。每张卡最多一个本轮 worker；训练代码软截止，外层进程组硬截止；仅管理本轮 PID。禁止外发 Git。
