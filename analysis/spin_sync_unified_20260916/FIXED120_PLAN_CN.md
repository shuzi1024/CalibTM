# 一次统一的固定120轮时长诊断

这是看到首轮曲线后追加的开发性诊断，不是独立验证。四项均预先纳入：两个WAN、full/no_memory、seed41001；只取消早停，固定到原定120轮上限。保持原模型、AdamW、固定学习率、裁剪、训练掩码、数据顺序、开发选择和初始化。

依据：GEANT no_memory 的11–20→21–30轮平均训练loss从0.233663降至0.222825（4.64%），平均开发NMAE从0.189555降至0.186884（1.41%）；末十轮最低分只比此前最低高0.093%，噪声可能让patience10过敏。Abilene末两段训练均值仅降0.46%，开发均值略升0.15%，单独不支持延长。旧SPIN曲线机械套本轮早停会令GEANT在29轮停于0.242551，而实际后来达到0.168317；其优化器、调度与监督不同，不能据此预测新模型必然恢复。

原四项按原规则独立完成且保留。每项完成后，使用其停止轮last.pt（不是best.pt）、优化器和RNG接续，继承完整历史与已选最佳权重；所有后续文件写独立fixed120命名空间，不修改原结果。父模型smoke与恢复能力已验证，新运行明确继承检查证据，不伪造一次新smoke。

当前原完整模型仍在训练，两项no_memory已完成。可以按父项完成时间分别接续，但四项共同采用同一个120轮预算；不根据谁获胜挑选接续对象。最多总计4GPU，绝对截止仍为北京时间2026-09-16 19:33:29（1789558409）。

预估GEANT完整模型全部120轮约3.3小时，当前时间仍允许完成此诊断。若120轮后Full仍无对照增量或整体缺乏竞争力，结束此轮结构，不继续改学习率、加模块或延长上限。是否重复初始化须在这次诊断结果后决定，不能提前把多个种子当成补救手段。
